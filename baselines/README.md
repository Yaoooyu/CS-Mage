# Baseline adapters

This directory documents the CS-Mage integration boundary for third-party
baselines. The CS-Mage wrappers are in `src/training/` and
`src/msa/run_mmsa_fivefold.py`. Obtain each upstream implementation at the
documented revision, apply the corresponding CS-Mage adapter, and retain the
upstream license and attribution notice.
