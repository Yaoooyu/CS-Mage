# Baseline adapters

This directory intentionally contains no third-party implementation source. The candidate provides the CS-Mage wrappers in `src/training/` and `src/msa/run_mmsa_fivefold.py`. Before release, clone each verified upstream repository at a documented revision, apply the documented CS-Mage adapter, and retain its original license.
